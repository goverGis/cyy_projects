# 区域智能划分系统 · 项目文件总览

> 路径：`E:\就业\项目\trae`（Windows 本机）
> 一句话定位：把离散的**业务事件**（闲置/寻物/急救/讨论 + POI 语义）按「业务量均衡 + 服务半径最短 + 形状紧凑」智能聚成若干**服务片区**，并用 Voronoi 边界落地到地图。
> 技术栈：React 18 + Vite + 高德 JSAPI 2.0（前端） / FastAPI + SQLAlchemy 2 + Pydantic v2（后端） / PostgreSQL 13 + PostGIS 3.2（生产数据） / numpy + shapely（算法）。

---

## 1. 目录结构（含逐文件说明）

```
trae/
├─ README.md                      产品级说明（技术栈/功能/架构/快速开始/算法/API）
├─ 项目文件总览.md                ← 本文件：逐文件清单与职责
├─ GITHUB_PUSH.md                 本机推送到 GitHub 的步骤与注意事项
├─ run.bat                        一键启动后端+前端（开两个窗口，8 秒后开浏览器）
├─ stop.bat                      按端口（8000/3000/3001/3002）结束两个服务
├─ showcase.html                 离线自包含简历作品集页（架构图+真实划分+亮点）
├─ package.json                  仓库根（仅占位/旧脚本引用）
├─ _agg.json / _agg2.json        旧聚合调试产物（非源码，可忽略）
├─ _items2.json                  旧事件导出样本（非源码，可忽略）
│
├─ api/                          后端 FastAPI
│  ├─ requirements.txt           依赖清单（fastapi/sqlalchemy/geoalchemy2/shapely/numpy/pytest…）
│  ├─ pytest.ini                 pytest 配置（测试发现与默认参数）
│  ├─ .env.example               数据库/配置模板（复制为 .env 填密码）
│  ├─ .env                       本地配置（已 gitignore，含真实 DATABASE_URL）
│  ├─ app/
│  │  ├─ main.py                 FastAPI 入口：CORS、挂载路由、/api/health
│  │  ├─ core/
│  │  │  ├─ config.py            Settings（DATABASE_URL / CORS / 默认 K、λ）
│  │  │  └─ database.py          SQLAlchemy 引擎、SessionLocal、Base、get_db
│  │  ├─ models/
│  │  │  ├─ event.py             Event（空间点+权重+POI类型）/ Plan / Territory / Assignment
│  │  │  └─ beijing.py           BeijingDistrict（生产级片区裁剪底图）
│  │  ├─ algorithms/
│  │  │  ├─ territory.py         容量约束划分核心：kpp 种子→Lloyd→move/swap 局部搜索→Voronoi→评估
│  │  │  └─ poi_divide.py        POI 语义划分：两阶段（DBSCAN 原子单元 + 容量约束聚合 + 超容递归拆分）
│  │  ├─ routers/
│  │  │  ├─ territory.py         /divide /timeseries /benchmark /poi-divide /poi-types /simulate
│  │  │  ├─ schemes.py           参数市场：方案的保存/列表/获取/删除（落盘 data/schemes.json）
│  │  │  ├─ events.py            PostGIS 事件 CRUD（GIST 空间过滤 bbox）
│  │  │  ├─ stats.py             /stats 按类型统计（PostGIS 驱动）
│  │  │  └─ legacy_items.py      文件型降级数据源（无 PG 时演示用）：/items、/items/aggregate、/plugins
│  │  ├─ schemas/
│  │  │  ├─ territory.py         划分/POI/推演 请求-响应模型
│  │  │  └─ event.py             Event 创建/更新/输出、StatsOut
│  │  └─ __init__.py
│  ├─ scripts/
│  │  ├─ seed.py                 将 data/events.json 灌入 PostGIS event 表（幂等）
│  │  ├─ load_beijing_districts.py  灌入北京行政区边界（真实边界或程序化近似）
│  │  └─ benchmark.py            命令行跑四基线对比
│  ├─ sql/
│  │  └─ 001_init.sql            建表 + PostGIS 扩展 + 空间索引
│  └─ tests/
│     ├─ test_territory.py       容量约束划分 / Voronoi / 评估指标 + PostGIS 集成
│     ├─ test_poi_divide.py      POI 两阶段 / 超容递归拆分 / 容量口径×1.2 回归
│     ├─ test_endpoints.py       /divide /poi-divide /simulate（points 直传，无 DB 可跑）
│     ├─ test_timeseries.py      时间滑块累积窗口（需 DB）
│     └─ __init__.py
│
├─ frontend/                     前端 React
│  ├─ package.json               依赖与脚本（dev/build/preview）
│  ├─ vite.config.js             Vite 配置：端口 3000 + /api 代理到 8000
│  ├─ index.html                 入口 HTML，内嵌高德 JSAPI key 占位
│  ├─ .env.example               高德 key / 安全密钥 / API base 模板
│  ├─ .env / .env.local          本地配置（已 gitignore）
│  ├─ dist/                      构建产物（npm run build 生成）
│  └─ src/
│     ├─ main.jsx                挂载 React 根
│     ├─ App.jsx                 路由 + 全局导航 + 错误边界 + 顶部 HUD（事件总数/时钟）
│     ├─ index.css               全局浅色自然风样式（CSS 变量：自然色系、卡片、地图装饰层）
│     ├─ pages/
│     │  ├─ HomePage.jsx         Hero 着陆页：真实数据讲清产品价值 + CTA
│     │  ├─ TerritoryPage.jsx    【核心】区域划分页：调参→划分→地图+卡片+报告+因果联动
│     │  ├─ DeveloperPage.jsx    参数市场：浏览/保存/对比/分享 (K,λ,μ,seed) 方案
│     │  ├─ MapView.jsx          全屏地图浏览：点按类型着色 + 抽稀/分页
│     │  ├─ SecondhandPage.jsx   闲置业务页（MiniMap + 增删改查事件）
│     │  ├─ LostFoundPage.jsx    寻物业务页
│     │  ├─ EmergencyPage.jsx    急救业务页
│     │  ├─ DiscussionPage.jsx   讨论业务页
│     │  └─ ClusterPage.jsx      旧聚类占位页（已并入区域划分页，保留路由兼容）
│     └─ components/
│        ├─ MapView.jsx          高德地图封装：点/片区渲染、抽稀、因果联动几何工具
│        ├─ MiniMap.jsx          轻量地图（业务页选点/预览用）
│        ├─ RegionCardList.jsx   【全局风格基准】片区卡片：类型色带/点数/权重/负载条/状态徽章/经纬度 + 排序筛选搜索
│        ├─ RegionLoadList.jsx   片区负载卡：容量进度条 + 预警 + 类型构成
│        ├─ RegionChipList.jsx   紧凑 chip 清单（复用自然色）
│        ├─ MetricPanel.jsx      通用指标面板（schema 驱动，可复用）
│        ├─ BalanceReport.jsx    均衡报告卡（与等距网格基线对比 + 结论）
│        └─ BenchmarkTable.jsx   四基线横向对比表格
│
├─ tools/
│  ├─ generator/                 事件造数器（Node）
│  │  ├─ generate.mjs            按北京真实热点高斯分布生成带权重的事件（可复现 seed）
│  │  └─ package.json
│  ├─ data/
│  │  ├─ events.json / .csv / .geojson  造数器输出（演示数据源）
│  └─ build_showcase.py          用真实划分结果重生成 showcase.html
│
├─ data/
│  ├─ events.json                （gitignore）4 类事件样本，演示模式数据源
│  ├─ events.csv / events.geojson 事件导出
│  ├─ beijing_districts.json     北京行政区边界（DataV GeoAtlas，GCJ-02）
│  └─ schemes.json               参数市场方案（运行时生成）
│
├─ backend/                      旧 Node/Express 原型（已弃用，保留作参考）
│  └─ src/algorithms/dbscan.js   原始 DBSCAN（仅出点簇、无片区边界）
│
├─ docs/
│  ├─ 01-现状审计与升级方案.md    v2 现状审计 + 演进方案（核心设计文档）
│  └─ 02-出彩改造设计文档.md      P0 出彩改造规划稿
│
└─ .github/workflows/ci.yml      CI：API 测试 + 覆盖率 + 前端 build
```

---

## 2. 后端（FastAPI）模块职责

| 文件 | 职责 | 关键接口/导出 |
|---|---|---|
| `app/main.py` | 应用入口，注册 CORS 与全部路由 | `GET /api/health` |
| `core/config.py` | 从 `.env` 读配置 | `DATABASE_URL`、`CORS_ORIGINS`、`DEFAULT_K` |
| `core/database.py` | 引擎/会话/基类 | `engine`、`SessionLocal`、`Base`、`get_db` |
| `models/event.py` | ORM 模型 | `Event`（空间点+`weight`+`poi_type`）、`Plan`、`Territory`、`Assignment` |
| `models/beijing.py` | 行政区底图 | `BeijingDistrict`（裁剪 Voronoi 用） |
| `algorithms/territory.py` | 容量约束划分主算法 | `divide()`、`random_divide`、`grid_divide`、`kmeans_divide`、`evaluate`、`haversine_m` |
| `algorithms/poi_divide.py` | POI 语义划分 | `poi_divide()`（两阶段 + 超容递归拆分） |
| `routers/territory.py` | 划分主路由 | `/divide`、`/timeseries`、`/benchmark`、`/poi-divide`、`/poi-types`、`/simulate` |
| `routers/schemes.py` | 参数市场 | `GET/POST /api/schemes`、`GET/DELETE /api/schemes/{id}` |
| `routers/events.py` | 事件 CRUD（PG） | `GET/POST/PUT/DELETE /api/events`、bbox 空间过滤 |
| `routers/stats.py` | 统计（PG） | `GET /api/stats` |
| `routers/legacy_items.py` | 文件型降级数据源 | `GET /api/items`、`GET /api/items/aggregate`、`/plugins` |
| `schemas/territory.py` | 划分请求/响应模型 | `DivideRequest/Response`、`PoiDivide*`,`Simulate*` |
| `schemas/event.py` | 事件模型 | `EventCreate/Update/Out`、`StatsOut` |
| `scripts/seed.py` | 灌库脚本 | `python -m scripts.seed [--append]` |
| `scripts/load_beijing_districts.py` | 灌行政区 | `python -m scripts.load_beijing_districts` |
| `sql/001_init.sql` | 建表 + PostGIS | 建 `event`/`territory`/`assignment`/`beijing_districts` + GIST 索引 |

### 数据流（一次划分）
```
前端 TerritoryPage
   │  调 POST /api/territory/divide（参数 k, λ, μ, seed, source, use_pg_voronoi, clip_to_district）
   ▼
routers/territory.py → _load_points_with_meta()
   │  点集来源：请求体 points > 数据库(auto/database) > 本地 data/events.json（自动降级）
   ▼
algorithms/territory.divide()  容量约束 Lloyd + move/swap + Voronoi
   │  若 use_pg_voronoi：用 PostGIS ST_VoronoiPolygons 生成边界（clip_to_district 再交行政区）
   ▼
_build_divide_result()  产出 regions + geojson + balance_report + recommendation
   │
   ▼ 返回前端 → 地图渲染片区 + RegionCardList 卡片 + BalanceReport
```

### 两种划分模式口径一致
- **容量约束模式** `/divide`：`capacity = 总权重 / k × 1.2`，`load_ratio = weight / capacity`。
- **POI 语义模式** `/poi-divide`：两阶段（DBSCAN 原子单元 → 容量约束聚合），`capacity` 同样按 `总权重 / target_k × 1.2`，故两种模式负载率/超载可同台对比。

---

## 3. 前端（React）模块职责

| 文件 | 职责 |
|---|---|
| `src/App.jsx` | 路由表（9 页）、顶部导航、错误边界、`/api/stats` 拉取全局事件数 |
| `src/index.css` | 浅色自然风主题（CSS 变量：森林绿/天蓝/暖阳橙/薰衣紫/珊瑚红） |
| `pages/TerritoryPage.jsx` | **核心页**：调参滑块 → 调 `/divide` → 地图 + 片区卡片 + 均衡报告 + 因果联动（点↔片 双向高亮）+ What-if 推演 |
| `pages/DeveloperPage.jsx` | 参数市场：方案列表/保存/排序/对比/生成可分享链接（`?k=&lam=&mu=&seed=` 还原大图） |
| `pages/HomePage.jsx` | Hero 着陆页，真实数据讲清价值 |
| `pages/MapView.jsx` | 全屏地图浏览（点按类型着色、抽稀、分页） |
| `pages/SecondhandPage` 等 4 个 | 业务页：MiniMap 选点 + 事件增删改查 |
| `components/MapView.jsx` | 高德封装：片区/点渲染、抽稀、射线法 `pointInRing`/`regionIdAtPoint` 因果联动 |
| `components/RegionCardList.jsx` | **全局风格基准**卡片：类型色带、点数/权重、负载条、状态徽章、经纬度、构成条；支持 `sortBy`/`statusFilter`/`poiType`/`keyword` 排序筛选搜索 |
| `components/RegionLoadList.jsx` | 容量模式负载卡（进度条 + 预警 + 构成） |
| `components/MetricPanel.jsx` | 通用指标面板（schema 驱动，可复用） |
| `components/BalanceReport.jsx` | 与等距网格基线对比的均衡结论 |
| `components/BenchmarkTable.jsx` | 随机/网格/k-means/容量约束 四基线对比 |
| `components/MiniMap.jsx` | 业务页轻量选点地图 |
| `components/RegionChipList.jsx` | 紧凑 chip 清单 |

---

## 4. 工具、数据、文档、CI

| 路径 | 作用 |
|---|---|
| `tools/generator/generate.mjs` | 北京真实热点高斯分布造数（固定 seed 可复现，输出 json/csv/geojson） |
| `tools/build_showcase.py` | 用真实 `/divide` 结果重生成 `showcase.html` |
| `data/events.json` | 演示模式数据源（4 类事件，~4000 条） |
| `data/beijing_districts.json` | 北京行政区边界（生产裁剪底图） |
| `docs/01-现状审计与升级方案.md` | 现状审计 + 演进方案（v2，设计依据） |
| `docs/02-出彩改造设计文档.md` | P0 出彩改造规划稿 |
| `README.md` | 产品级说明（含快速开始、算法、API 速览） |
| `GITHUB_PUSH.md` | 本机推 GitHub 步骤 |
| `run.bat` / `stop.bat` | 一键启动 / 停止（CRLF + 纯 ASCII，避免 cmd 解析坑） |
| `.github/workflows/ci.yml` | API 测试 + 覆盖率 + 前端 build |

---

## 5. 演示模式 vs 生产模式

| 维度 | 演示模式 | 生产模式 |
|---|---|---|
| 数据源 | `data/events.json` | PostGIS `event` 表 |
| 边界 | shapely 本地 Voronoi | `ST_VoronoiPolygons`（可再裁剪到北京行政区） |
| 依赖 | 仅需 FastAPI | 需 PostgreSQL + PostGIS |
| 切换 | `source=auto` 自动按可用性选择 | 配好 `DATABASE_URL` 即生效（连不上自动回退本地文件） |

---

## 6. 启动指令（速查）

> 详见下方对话框说明。`run.bat` 双击即可一键起前后端；也可分步手动启动。
