# FastAPI 后端依赖（销售片区 / 区域智能划分系统）
# 安装：python -m venv .venv && .venv/Scripts/pip install -r requirements.txt

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# 数据库与空间类型
sqlalchemy>=2.0,<3.0
geoalchemy2>=0.15,<1.0
psycopg[binary]>=3.1,<4.0

# 几何与数值计算（算法层）
shapely>=2.0,<3.0
numpy>=1.26,<3.0

# 配置与校验
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0

# 测试
pytest>=8.0
httpx>=0.27
