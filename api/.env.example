# 后端数据库连接串（PostGIS）
# 复制本文件为 .env 后，把密码改成你本机 PostgreSQL 的 postgres 用户密码
# 例：postgresql+psycopg://postgres:你的密码@localhost:5432/webgis_territory
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/webgis_territory

# 可选：SQL 回声（调试用，默认关闭）
# SQL_ECHO=false
