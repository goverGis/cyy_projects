# 高德地图 JSAPI 2.0 配置
# 复制本文件为 .env 后填入自己的 key
#
# 说明：JSAPI 的 key 本质上会随前端代码下发到浏览器，无法真正保密。
# 真正的安全手段是在高德控制台配置「域名白名单 / Referer 限制」。
# 这里不入库只是避免把个人账号的 key 提交到公开仓库。
#
# 申请地址：https://console.amap.com/dev/key/app  →  创建新应用 → 添加 Key → 服务平台选「Web端(JS API)」

VITE_AMAP_KEY=你的高德JSAPI_KEY
VITE_AMAP_SECURITY_KEY=你的高德安全密钥

# 后端 API 地址（FastAPI，默认 8000）
VITE_API_BASE=/api
